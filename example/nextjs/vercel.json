{
  "buildCommand": "cd ../.. && npm ci --include=dev && npm run build && cd example/nextjs && npm ci --include=dev && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
