{
  "buildCommand": "cd ../.. && npm install && npm run build && cd example/vite && npm install && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite"
}
